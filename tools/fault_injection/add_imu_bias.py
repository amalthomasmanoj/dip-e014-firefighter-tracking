from __future__ import annotations


def add_imu_bias(records: list[dict], accel_bias_mps2: tuple[float, float, float]) -> list[dict]:
    raise NotImplementedError("IMU bias injection is future work.")

